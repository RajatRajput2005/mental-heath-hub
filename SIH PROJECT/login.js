// main.js
// script.js

function login() {
    var userType = document.getElementById('user_type').value;
    var username = document.getElementById('username').value;
    var password = document.getElementById('password').value;

    // Perform login validation here (not implemented in this example)
    // You can make an AJAX request to the server for validation
    // For simplicity, we'll just show a message here
    alert('Login function called');
}

function signup() {
    var signupUserType = document.getElementById('signup_user_type').value;
    var signupUsername = document.getElementById('signup_username').value;
    var signupPassword = document.getElementById('signup_password').value;

    // Perform signup validation here (not implemented in this example)
    // You can make an AJAX request to the server for validation
    // For simplicity, we'll just show a message here
    alert('Signup function called');
}
